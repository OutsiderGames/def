# def
Deadly Enemy Force

#Manager
* BrickManger
  - getColor(float rate)
  - drawNewBrick(Vector3 position)
* BallManager
  - receiveBall(Ball ball) 
* TurnManger
  - start()
  - restart()
  - increaseTurn()
  - getTurn()
* ScoreBoardManager
  - increaseScore()
